export const environment = {
  production: true,
  apiUrl: 'https://ted-be-dev.fly.dev/api/v1',
  // Base URL for absolute canonical / OG / JSON-LD URLs (Phase 10 SEO).
  // Pre-launch: replace with the real canonical domain (also update Fly
  // STOREFRONT_URL + public/robots.txt Sitemap line to match).
  siteUrl: 'https://ted-clothing-o0ea301pc-aman-goyal-s-projects.vercel.app'
};
